name: Update scores

on:
  schedule:
    - cron: '0 */6 * * *'   # every 6 hours (UTC)
  workflow_dispatch:          # lets you run it on demand from the Actions tab

permissions:
  contents: write             # needed to commit & push the updated results.json

concurrency:
  group: update-scores
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch pool results from football-data.org
        env:
          FOOTBALL_DATA_TOKEN: ${{ secrets.FOOTBALL_DATA_TOKEN }}
        run: node scripts/fetch-results.js

      - name: Fetch Match Center data (standings, scorers, matches)
        env:
          FOOTBALL_DATA_TOKEN: ${{ secrets.FOOTBALL_DATA_TOKEN }}
        run: node scripts/fetch-features.js

      - name: Commit data if it changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add results.json data/standings.json data/scorers.json data/matches.json
          if git diff --staged --quiet; then
            echo "No changes — data is already up to date."
          else
            git commit -m "chore: update results & match data [skip ci]"
            git push
          fi
